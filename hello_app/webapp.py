"""
This module defines the Flask web application for the "Hello, Flask!" project.

It imports the Flask application instance from the `app` module and the views
module that defines the routes and view functions for the application.
"""
from . import app
from . import views
